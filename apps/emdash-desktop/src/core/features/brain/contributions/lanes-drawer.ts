/** What the Lanes view mounts from the Brain slice (another slice's `browser/` is off limits). */
export { BrainDrawer, type BrainDrawerProps } from '../browser/brain-drawer';
export { BrainTitlebarControls } from '../browser/titlebar-controls';
export { LaneRunModeControl } from '../browser/lane-run-mode';
export { stopAllAgentWork } from './stop-action';
