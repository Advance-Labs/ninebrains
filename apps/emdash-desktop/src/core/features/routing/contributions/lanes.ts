/** What the Lanes view mounts from the routing slice (another slice's `browser/` is off limits). */
export {
  LaneRoutingControl,
  LaneRoutingFields,
  type LaneRoutingValue,
} from '../browser/lane-routing';
